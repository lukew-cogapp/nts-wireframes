import { t } from "@/lib/strings";

export const JOIN_STEPS = [
	t("join.progress.payment"),
	t("join.progress.details"),
	t("join.progress.address"),
	t("join.progress.pay"),
	t("join.progress.done"),
];

export const DONATE_STEPS = [
	t("donate.progress.amount"),
	t("donate.progress.details"),
	t("donate.progress.address"),
	t("donate.progress.payment"),
];
